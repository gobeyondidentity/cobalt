# Aegis configuration
# Required: Nexus server URL and DPU name
AEGIS_SERVER=https://nexus.example.com:8443
AEGIS_DPU_NAME=bf3-001

# DOCA ComCh Configuration (optional, enables high-performance transport)
# On BlueField-3, ConnectX-7 controllers are typically at 03:00.0 and 03:00.1
# Uncomment and set these to enable DOCA ComCh instead of tmfifo fallback
#DOCA_PCI_ADDR=03:00.0
#DOCA_REP_PCI_ADDR=03:00.1
#DOCA_SERVER_NAME=secure-infra
