# Aegis - Secure Infrastructure DPU Agent Configuration
# See documentation: https://github.com/gobeyondidentity/secure-infra
#
# This file is sourced by systemd. Uncomment and edit values as needed.
# After changes, run: systemctl daemon-reload && systemctl restart aegis

# Required: Nexus control plane server URL
AEGIS_SERVER=https://nexus.example.com:8443

# Required: Unique name for this DPU (must match registration in Nexus)
AEGIS_DPU_NAME=bf3-001

# Optional: gRPC listen address (default: :18051)
# AEGIS_LISTEN=:18051

# Optional: Local API listen address for host agent communication (default: localhost:9443)
# AEGIS_LOCAL_LISTEN=localhost:9443

# Optional: SQLite database path for state persistence (default: /var/lib/aegis/aegis.db)
# AEGIS_DB_PATH=/var/lib/aegis/aegis.db

# Optional: TOFU keystore path for host authentication (default: /var/lib/secureinfra/known_hosts.json)
# AEGIS_KEYSTORE=/var/lib/secureinfra/known_hosts.json

# Optional: BMC address for Redfish API (leave empty to disable)
# AEGIS_BMC_ADDR=192.168.1.203
# AEGIS_BMC_USER=root
